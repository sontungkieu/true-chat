# Benchmark Results

This directory contains selected historical benchmark snapshots.

New generated BudgetRAG outputs are ignored by default under `benchmark_results/budgetrag/`. Commit only curated summaries under `docs/reports/` unless a raw benchmark artifact is explicitly selected for review.
